local numeros = {}

for i = 1, 100 do
    numeros[i] = math.random(1, 100)
end

local countPares = 0

for i = 1, #numeros do
    if numeros[i] % 2 == 0 then
        countPares = countPares + 1
    end
end

print("quantidade de numeros pares " .. countPares)
